REM If your regular Java version is below 11, uncomment the line with "SET" and
REM add the path of a Java executable with version 11 or higher to the Windows
REM PATH environment variable. That is, because at least Java 11 is required to
REM run the static analyzer.

REM SET PATH=C:\Program Files\Java\jdk-13.0.1\bin;%PATH%
